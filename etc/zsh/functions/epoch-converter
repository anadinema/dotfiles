local ts="${1:-$(date +%s)}"

if [[ "$OSTYPE" == "linux-gnu"* || "$OSTYPE" == "msys"* ]]; then
  date -u --date "@$ts" '+%Y-%m-%dT%H:%M:%SZ'
elif [[ "$OSTYPE" == "darwin"* ]]; then
  date -u -r "$ts" '+%Y-%m-%dT%H:%M:%SZ'
else
  echo "⁉️ unsupported OS for date conversion"
  return 1
fi
