version: 1
git_protocol: ssh
prompt: enabled
prefer_editor_prompt: disabled
color_labels: enabled
accessible_colors: disabled
accessible_prompter: disabled
spinner: enabled
aliases:
  co: pr checkout
  cpr: pr create --fill --web
  rem: repo view --web
